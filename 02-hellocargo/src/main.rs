// ------------------------------------------------------------------
// Cargo expects your source files to live inside the src directory.
// The top-level project directory is just for README files, license
// config files, and anything else NOT related to your code.
// ------------------------------------------------------------------
fn main() {
  println!("Hello, world!");
}
